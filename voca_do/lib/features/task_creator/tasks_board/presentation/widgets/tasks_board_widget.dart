 import 'package:flutter/material.dart';

class TasksBoardWidget extends StatelessWidget {
  const TasksBoardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
  }
